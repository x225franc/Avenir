/**
 * Module SEO - Métadonnées et optimisation pour les moteurs de recherche
 */

export { useClientMetadata } from "./client";
export { generatePageMetadata, defaultMetadata, pagesMetadata, type PageMetadata } from "./config";
